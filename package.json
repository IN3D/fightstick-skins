{
  "name": "fightstick-skins",
  "version": "0.1.0",
  "description": "CSS Skins for OBS arcadestick overlays",
  "main": "index.js",
  "scripts": {
    "test": "echo \"What are you trying to test exactly??\" && exit 0",
    "build": "sass --no-source-map scss:out",
    "minify": "cleancss -O2 --batch --batch-suffix '' out/**/*.css"
  },
  "author": "Eric Hopkins",
  "license": "ISC",
  "dependencies": {
    "clean-css-cli": "^5.6.0",
    "sass": "^1.52.3"
  }
}
